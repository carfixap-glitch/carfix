"use client";

import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase";

export default function AssessmentPage({params}:{params:{id:string}}){ const [a,setA]=useState<any>(null); const [vehicle,setVehicle]=useState<any>(null); const [photos,setPhotos]=useState<any[]>([]); const [loading,setLoading]=useState(true); const supabase=createClient(); useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser(); if(!user){window.location.href="/login";return;} const {data,error}=await supabase.from("assessments").select("*, vehicles(*)").eq("id",params.id).eq("user_id",user.id).single(); if(error){setLoading(false);return;} setA(data);setVehicle(data.vehicles); const {data:ps}=await supabase.from("assessment_photos").select("*").eq("assessment_id",params.id); if(ps){const withUrls=await Promise.all(ps.map(async p=>{const {data:u}=await supabase.storage.from("carfix-damage-photos").createSignedUrl(p.storage_path,3600);return {...p,url:u?.signedUrl}}));setPhotos(withUrls)} setLoading(false)})()},[params.id]); if(loading)return <main className="section"><div className="container">Loading assessment...</div></main>; if(!a)return <main className="section"><div className="container"><h1>Assessment not found</h1><a href="/dashboard">Back to dashboard</a></div></main>; return <main><header className="nav"><div className="container"><div className="brand"><span>Car</span>Fix</div></div></header><section className="section"><div className="container"><a href="/dashboard">← Dashboard</a><p className="muted" style={{marginTop:25}}>Assessment</p><h1>{vehicle?.make} {vehicle?.model}</h1><p className="muted">{a.city} · Status: {a.status}</p><div className="card" style={{marginTop:25}}><h2>Uploaded damage photos</h2><div className="grid" style={{gridTemplateColumns:"repeat(3,1fr)",marginTop:18}}>{photos.map(p=><div key={p.id} className="card" style={{padding:8}}>{p.url&&<img src={p.url} alt="Car damage" style={{width:"100%",height:180,objectFit:"cover",borderRadius:8}}/>}</div>)}</div><div style={{marginTop:25}}><h2>AI analysis</h2><p className="muted">Your photos are saved. AI damage analysis will be connected next; no repair result is being invented before the analysis runs.</p></div></div></div></section></main>; }
